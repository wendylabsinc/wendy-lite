dependencies:
  wendy_conf:
    override_path: "../wendy_conf"
  wendy_stdio:
    override_path: "../wendy_stdio"
  wendy_wasm:
    override_path: "../wendy_wasm"
    rules:
      - if: "$CONFIG{WENDY_CORE_WASM_APP_SUPPORT} == True"
  wendy_hal:
    override_path: "../wendy_hal"
    rules:
      - if: "$CONFIG{WENDY_CORE_WASM_APP_SUPPORT} == True"
  wendy_hal_export:
    override_path: "../wendy_hal_export"
    rules:
      - if: "$CONFIG{WENDY_CORE_WASM_APP_SUPPORT} == True"
  wendy_usj:
    override_path: "../wendy_usj"
  wendy_wifi:
    override_path: "../wendy_wifi"
