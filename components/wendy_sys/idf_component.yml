dependencies:
  espressif/wasm-micro-runtime:
    version: "~2.4.0"
    rules:
      - if: "$CONFIG{WENDY_WASM} == True"
  wendy_callback:
    override_path: "../wendy_callback"
  wendy_wasm:
    override_path: "../wendy_wasm"
